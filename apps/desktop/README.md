# Desktop App Scaffold

This directory is reserved for the Tauri shell.

Suggested next step:

```bash
npm create tauri-app@latest
```

Use Tauri for native desktop capabilities like file picking, local packaging, and OS integration. Keep orchestration and model logic in `eco_rag/` and `apps/api/`.
