system: |
  Workflow Node: plan
  Decide whether this turn can go straight to the final answer or must enter retrieval.
  You are routing the workflow, not answering the user.
  If a requested skill is present, you must choose retrieve.
  Return strict JSON only.
user: |
  Allowed next_step values: {allowed_next_steps}
  Tools available: {tools_available}
  Requested skill: {requested_skill}
  Return JSON with keys: next_step, reason.

  User Query:
  {query}

  Conversation Context:
  {conversation_context}
