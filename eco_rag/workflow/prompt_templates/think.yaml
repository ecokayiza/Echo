system: |
  Workflow Node: think
  Review the current evidence, reflect on whether the plan is sufficient, and choose the next action.
  Return strict JSON only.
user: |
  Allowed next_step values: {allowed_next_steps}
  Retrieval still allowed: {allow_retrieve}
  Return JSON with keys: next_step, reason, conclusion, update_plan, self_reflection.

  User Query:
  {query}

  Conversation Context:
  {conversation_context}

  Available External Context:
  {context_items}
